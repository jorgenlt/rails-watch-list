<div class="container">
  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), patternpad.svg;">
    <div class="container">
      <h1>Welcome to The Movie Lister</h1>
      <p>Make your own lists of movies!</p>
    </div>
  </div>



  <div class="d-flex justify-content-center">
    <div id="lists ">
      <div class="grid-index">
        <% @lists.each do |list| %>
          <%= link_to list_path(list) do %>
            <div class="item-div">
            <div><%= cl_image_tag list.photo.key, height: 300, width: 300, crop: :fill %></div>
              <div class="item">
                <h5><%= list.name %></h5>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
